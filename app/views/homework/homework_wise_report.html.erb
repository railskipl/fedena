<div id="content-header">
   <img src="/images/homework/button_homeworks.gif" alt="homework" />
  <h1>Homeworks</h1>
  <h3>Homework Wise Reports</h3>
  <div id="app-back-button">
    <%= link_to_function image_tag("/images/buttons/back.png",:border => 0), "history.back()" %>
  </div>
</div>
<div id="page-yield">

    <% unless flash[:notice].nil? %>
    <p class="flash-msg"> <%= flash[:notice] %> </p>
  <% end %>

 <div class="box">
<% form_for :homework_report, :url=>{:action=>'generated_report'} do |x| %>

<div class="text-input-bg">
<%= select :homework_report, :batch_id,
        @batches.map {|b| [b.full_name, b.id]},
        {:prompt => 'Select a batch'},
        {:onChange => "#{remote_function(:url => {:action => "list_homework_types"},
        :with => "'batch_id='+value",
        :before => "Element.show('loader')",
        :success => "Element.hide('loader')"
        )}"} %>
</div>

  <div  class="text-input-bg" id="homework-group-select">
    <%= select :homework_report, :homework_group_id, @homework_groups.map{|homework| [homework.name,homework.id]},:prompt=>'Select homework group' %>
  </div>
<%= image_tag("loader.gif",
      :align => "absmiddle",
      :border => 0,
      :id => "loader",
      :style =>"display: none;" ) %>

    <%= submit_tag "", :value => "► View", :class => "submit_button" %>
  <% # x.submit 'View' %>

<% end %>

  <div class="extender">

      </div>
 </div>
    </div>