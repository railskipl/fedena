

<div id="homework_result">
  
    <% form_for :homework_result, :url => {:action => "save", :controller => "homework_result"} do |res| %>

      <%- @students.each do |s| -%>
        <%- x = homeworkResult.find_by_homework_id_and_student_id(@exm.id, s.id); m = x.marks unless x.nil? -%>
       
        <div class="st-marks-block">
          <div class="st-name<%=cycle('odd', 'even')%>"> <%= s.first_name %> <%= s.last_name %> </div>
          <div class="marks-input"> <%= res.text_field :marks, :index => s.id, :value => m, :size => 3 %> </div>

        </div>
    
      <%- end -%>

      <div id="submit-area">
          
        <%= hidden_field_tag :homework_id, @exm.id %>

        <%= submit_tag "", :value => "► Update" %>
      </div>
      <div class="extender"> </div>
    <%- end -%>
  
</div>

